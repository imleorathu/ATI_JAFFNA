{
  "scripts": {
    "dev:full": "concurrently -k -n backend,frontend -c blue,green \"npm --prefix Backend run dev\" \"npm --prefix Frontend run dev\""
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.15.0",
    "pdfkit": "^0.18.0",
    "pdfmake": "^0.3.9"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
